function operator (proxies) {

      const counter = {};

      return proxies.map(p => {

        if (!counter [香港]) counter [香港] = 0;

        p.name = p. name + " " + (++counter [香港]).toString();

        return p;

      });

  }
