更新系统：
```
apt-get update -y && apt-get install curl wget vim dnsutils telnet -y
```

安装：
```
bash <(curl -sSL https://gitee.com/mb9e8j2/Tools/raw/master/Linux_reinstall/InstallNET.sh) --network "static" -debian 11 -pwd 'passwd'
```
